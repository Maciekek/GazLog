services:
  gazlog:
    build: .
    image: gazlog
    container_name: gazlog
    restart: unless-stopped
    env_file: .env
    ports:
      - "3001:3001"
    volumes:
      - gazlog-data:/data

volumes:
  gazlog-data:
