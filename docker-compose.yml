services:
  gazlog:
    build:
      context: .
      args:
        VITE_CONTACT_EMAIL: ${VITE_CONTACT_EMAIL:-}
    image: gazlog
    container_name: gazlog
    restart: unless-stopped
    env_file: .env
    ports:
      - "3001:3001"
    volumes:
      - gazlog-data:/data

volumes:
  gazlog-data:
